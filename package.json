{
  "name": "blog",
  "version": "1.0.0",
  "description": "test",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "build": "gulp restore-ignore && gulp prebuild && gitbook build && gulp postbuild",
    "preview": "gulp preview",
    "upload": "gulp publish --dest /Volumes/creator/manual",
    "publish": "npm run build && npm run upload"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Garretming/blog.git"
  },
  "keywords": [
    "gitbook"
  ],
  "author": "clark'",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Garretming/blog/issues"
  },
  "homepage": "https://github.com/Garretming/blog#readme",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "node-sass": "^4.12.0",
    "sass-loader": "^7.1.0"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "gulp": "^4.0.2"
  }
}
